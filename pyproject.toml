[project]
name = "simple-rag-serving"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Strath Lumsden", email = "strath.lumsden@outlook.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
simple-rag-serving = "simple_rag_serving:main"

[build-system]
requires = ["uv_build>=0.11.18,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.16",
]
